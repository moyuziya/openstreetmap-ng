name: Run tests

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.implementation }} tests (${{ matrix.os }})
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        implementation: [python, cython]
        exclude:
          - os: macos-latest
            implementation: cython
      fail-fast: false

    timeout-minutes: 60
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 9310e60f49b21c378610fbbe86f5a66b27bb011e
          persist-credentials: false
      - uses: ./.github/actions/setup

      - name: Build Cython modules
        if: matrix.implementation == 'cython'
        run: |
          nix-shell --pure --run "cython-build-fast"

      - name: Start services
        run: |
          nix-shell --pure --run "dev-start"

      - name: Run tests
        run: |
          nix-shell --pure --run "run-tests --extended --term"
